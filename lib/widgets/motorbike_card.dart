import 'package:flutter/widgets.dart';

class MotorbikeCard extends StatelessWidget {
  const MotorbikeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
